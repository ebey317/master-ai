// {{NAME}} — client-side glue
// Generated by AppForge on {{GENERATED_AT}}.
console.log("{{NAME}} loaded — {{TAGLINE}}");

// Hook features from the server (already rendered server-side too — this
// lets you refresh dynamically from /api/features later).
fetch("/api/features")
  .then(r => r.json())
  .then(list => console.log("features:", list))
  .catch(e => console.error(e));
